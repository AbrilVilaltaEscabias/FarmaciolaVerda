[project]
name = "Farmaciola Verda"
version = "0.1.0"
description = ""
author = [{name = "Abril Vilalta"}]
readme = README.md
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"